const User = require('../../src/app/models/User')

const bcrypt = require('bcryptjs')

describe('User', () => {
  beforeEach(async () => {

  })

  it('should created user', async () => {

  })

  it('should authenticated', async () => {

  })

  it('should password invalid', async () => {

  })

  it('should email is existing', async () => {

  })
})
